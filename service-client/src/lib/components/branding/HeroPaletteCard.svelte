<!--
	Hero card for global agency branding.

	The internal preview MUST use globals-only branding (agency.{primaryColor,
	secondaryColor, accentColor, accentGradient, logoUrl}) — NOT the resolver
	output of getEffectiveProposalBranding(agencyId), which folds in
	proposal-level overrides. The hero represents the global brand identity;
	mixing in proposal overrides confuses agencies who set them and breaks the
	"this is what your global brand looks like" semantic. See branding-workspace-v2
	plan, Cowork appraisal Flag 1 (2026-04-25).
-->
<script lang="ts"></script>
